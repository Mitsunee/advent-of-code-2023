export interface NumberInfo {
  /**
   * value as number
   */
  value: number;
  /**
   * first idx of the number in the string
   */
  start: number;
  /**
   * last idx of the number in the string
   */
  end: number;
}
